services:
  rnieb-backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/app/data/rnieb.db # path of db in container
      - UPLOAD_PATH=/app/data/uploads/ # uploads destination
      - DOTENV_PATH=/app/data/.env
    volumes:
      - ./data:/app/data/ # mounts ./data as /app/data in container
